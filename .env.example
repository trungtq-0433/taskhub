ENVIRONMENT=local
DATABASE_URL=postgresql+asyncpg://<username>:<password>@localhost:5432/<database_name>
DEBUG=true

# Read by docker compose only. Must match the port in DATABASE_URL above.
POSTGRES_HOST_PORT=5432
